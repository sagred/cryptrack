import axios from 'axios';
import crypto from 'crypto';

export const getCoinbaseBalance = (oauthToken) => {
    // Call Coinbase API using the OAuth token to get the balance
    // Return the balance
}
